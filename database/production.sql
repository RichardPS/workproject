CREATE TABLE sqlite_sequence(name,seq);
CREATE TABLE production (
    "pro_id" INTEGER NOT NULL,
    "pro_month" DECIMAL,
    "pro_year" DECIMAL,
    "pro_firstline" VARCHAR,
    "pro_issues" INTEGER
);
